# Local/dev only. Real secrets live in Supabase Function secrets and EAS secrets.
# The client never sees a PSP key and never calls a PSP directly (01-architecture.md §3).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_DB_URL=
