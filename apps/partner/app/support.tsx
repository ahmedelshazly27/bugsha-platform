// S-P-008 / S-P-039 — training index and partner support (faster than consumer, and says so).
import { router } from 'expo-router'; import { Linking, View } from 'react-native';
import { AppBar, Caption, Card, Eyebrow, ListRow, Pp, Screen, T } from '@bugsha/ui'; import { useL } from '../src/lib/ui';
export default function Support() { const { L } = useL();
  const topics: Array<[string, string, string, string]> = [['package', 'Listing in 15 seconds', 'العرض خلال 15 ثانية', 'Pick a template, set tonight\'s quantity, confirm the window. Price is inherited.'], ['qr-code', 'Redeeming at the counter', 'الاستلام عند الكاشير', 'Type the code or scan. Works without network — it syncs when you reconnect.'], ['clock', 'Late arrivals', 'الوصول المتأخر', 'Within 30 minutes of close you can still redeem from the board. After that it is a no-show and you keep the revenue.'], ['banknote', 'Cash orders', 'الطلبات النقدية', 'Collect the exact amount shown. Commission on cash is invoiced monthly, never deducted from card payouts.'], ['triangle-alert', 'Quality concerns', 'مخاوف الجودة', 'Unsure about food? Cancel the listing with "quality concern" — customers are refunded and nothing counts against you beyond the cancellation.']];
  return <View style={{ flex: 1 }}><AppBar title={L('Help & training', 'المساعدة والتدريب')} onBack={() => router.back()} /><Screen top={false} gap={10}>
    <Eyebrow>{L('Training', 'التدريب')}</Eyebrow>{topics.map(([i, en, a, body]) => <Card key={en}><View style={{ gap: 4 }}><View style={{ flexDirection: 'row', gap: 8, alignItems: 'center' }}><T weight={600}>{L(en, a)}</T></View><Pp>{body}</Pp></View></Card>)}
    <Eyebrow>{L('Partner support', 'دعم الشركاء')}</Eyebrow><Card padded={false}><ListRow icon="info" label="WhatsApp" value={<Caption>{L('Replies in ~5 min during service', 'الرد خلال 5 د أثناء الخدمة')}</Caption>} chevron onPress={() => Linking.openURL('https://wa.me/96500000000')} /><ListRow icon="info" label={L('Email', 'البريد')} value={<Caption>partners@bugsha.com</Caption>} chevron onPress={() => Linking.openURL('mailto:partners@bugsha.com')} /></Card></Screen></View>; }
